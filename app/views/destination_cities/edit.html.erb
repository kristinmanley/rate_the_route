<!-- Validation failure messages -->
<% if @destination_city.errors.any? %>
  <% @destination_city.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>

<div class="page-header">
  <h3>Edit Destination City #<%= @destination_city.id %></h3>
</div>

<div class="row">
  <div class="col-md-12">
    <form action="/update_destination_city/<%= @destination_city.id %>" method="post">
      <!-- Hidden input for authenticity token to protect from forgery -->
      <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">

      <button class="btn btn-warning">
        Update Destination City
      </button>
      or
      <a href="/destination_cities/<%= @destination_city.id %>">Cancel</a>
    </form>
  </div>
</div>
