<div class="page-header">
  <h1>
    Flights
    <a href="/flights/new" class="btn btn-lg btn-success">New Flight</a>
  </h1>
</div>

<div class="row">
  <div class="col-md-12">
    <table class="table table-striped table-hover">
      <tr>
        <th>Destination city</th>
        <th>Departure city</th>
        <th>Destination airport</th>
        <th>Departure airport</th>
        <th>Airline</th>
        <th>Actions</th>
      </tr>

      <% @flights.each do |flight| %>
      <tr>
        <td><%= flight.destination_city.created_at %></td>
        <td><%= flight.departure_city.created_at %></td>
        <td><%= flight.destination_airport.created_at %></td>
        <td><%= flight.departure_airport.created_at %></td>
        <td><%= flight.airline_id %></td>
        <td>
          <a href="/flights/<%= flight.id %>" class="btn btn-primary">Show</a>
          <a href="/flights/<%= flight.id %>/edit" class="btn btn-warning">Edit</a>
          <a href="/delete_flight/<%= flight.id %>" class="btn btn-danger" rel="nofollow">Delete</a>
        </td>
      </tr>
      <% end %>
    </table>
  </div>
</div>
